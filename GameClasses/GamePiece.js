class GamePiece {
    //Need to figure out how to make this a button that can be enabled/disabled
    constructor(color){
        this.color = color;
    }
    
    setLocation(row, column){
        this.row = row;
        this.column = column;
    }
}